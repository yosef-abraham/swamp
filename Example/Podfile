use_frameworks!
platform :ios, 10.1
target 'Swamp_Test-iOS' do
  inherit! :search_paths
  pod 'Swamp', :path => '../'

  pod 'Quick'
  pod 'Nimble'
end

target 'Swamp_Test-OSX' do
    inherit! :search_paths
    pod 'Swamp', :path => '../'

    pod 'Quick'
    pod 'Nimble'
end
