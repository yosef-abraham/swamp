use_frameworks!

target 'Swamp_Test-iOS' do
  inherit! :search_paths
  pod 'Swamp', :path => '../'

  pod 'Quick', '~> 0.9.3'
  pod 'Nimble', '~> 4.1.0'
end

target 'Swamp_Test-OSX' do
    inherit! :search_paths
    pod 'Swamp', :path => '../'
    
    pod 'Quick', '~> 0.9.3'
    pod 'Nimble', '~> 4.1.0'    
end
